using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MaxFIrstScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Repo one"); 
        Debug.Log("Repo2");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
